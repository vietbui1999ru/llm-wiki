# git worktrees (optional local dev setup — see GUIDE.md)
.worktrees/

# qmd index (local, rebuilt per machine)
.qmd/

# LightRAG graph index (local, rebuilt via wiki-index)
.lightrag/
*.sqlite
*.sqlite-shm
*.sqlite-wal
~/.cache/qmd/

# macOS
.DS_Store

# Raw source documents (local only — not tracked on remote)
raw/

# Assets are tracked but large — add to LFS later if needed
# assets/
.env
.mcp.json
.obsidian
.claude
.claude/pre-digest/
.playwright-mcp/
docs-site/.mintlify/
/tmp/mint-dev.log
