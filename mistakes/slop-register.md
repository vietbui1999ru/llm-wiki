# Slop Register

Patterns this AI consistently gets wrong in *this* codebase. Distinct from `global-prevention-rules.md` (which covers operational mistakes like wrong CLI flags).

Populated manually via `/capture-slop` after code review, bugs, or `/diagnose` sessions.
Injected into agent context at startup when this file has entries.

**Format per entry**: pattern name → what AI does wrong → correct behavior → how to detect.

---

## Naming & Conventions

*(empty — add entries via /capture-slop)*

## Error Handling

*(empty)*

## Module Boundaries

*(empty)*

## Dependencies & APIs

*(empty)*

## Patterns Used Incorrectly

*(empty)*
